#!/usr/bin/env ruby

$LOAD_PATH << File.expand_path(File.join(File.dirname(__FILE__), "..", "lib"))

require 'git-invert'

GitInvert::CommitReader.each_commit(git_dir: '/Users/rachel/git/github.com/rvedotrc/dropbox-hacking/.git', revisions: nil) do |x|
  p [x.commit, x.tree, x.parents]
end
